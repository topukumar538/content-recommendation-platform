# secrets — Compose injects config as environment variables at runtime,
# so the file itself must never reach the build context
.env
.env.env

# test artifacts
test.db
backend/tests/load/results/

# local virtualenv
venv/

# Python cache
__pycache__/
*.py[cod]